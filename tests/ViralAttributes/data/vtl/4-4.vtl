
    define viral propagation CONF (variable VAt_1) is
        when "C" then "C";
        when "N" then "N";
        else "F"
    end viral propagation;
    DS_r <- inner_join(DS_1 as a, DS_2 as b calc Me_3 := a#Me_1 + b#Me_1);
