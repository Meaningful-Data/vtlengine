define hierarchical ruleset accountingEntry (variable condition REF_AREA rule ACCOUNTING_ENTRY) is 
    when REF_AREA = "IT" then B = C - D errorcode "Balance(credit-debit)" errorlevel 4;
    N > A + L errorcode "Net(assets-liabilities)" errorlevel 4    
end hierarchical ruleset;

DS_r := check_hierarchy(BOP, accountingEntry always_zero);