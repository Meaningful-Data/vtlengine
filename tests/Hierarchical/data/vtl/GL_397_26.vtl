define hierarchical ruleset accountingEntry (valuedomain condition VD_REF_AREA as CNTRY rule ACCOUNTING_ENTRY_vd) is 
    when CNTRY = "IT" then B = C - D errorcode "Balance(credit-debit)" errorlevel 4;
    when CNTRY = "IT" then N = A + L errorcode "Net(assets-liabilities)" errorlevel 4    
end hierarchical ruleset;

DS_r := hierarchy(BOP, accountingEntry condition REF_AREA rule ACCOUNTING_ENTRY non_null);