DS_r := sum(DS_1 group all time_agg("D", last));
DS_r1 := sum(DS_1 group all time_agg("W", last));
DS_r2 := sum(DS_1 group all time_agg("M", last));
DS_r3 := sum(DS_1 group all time_agg("Q", last));
DS_r4 := sum(DS_1 group all time_agg("S", last));
DS_r5 := sum(DS_1 group all time_agg("A", last));
