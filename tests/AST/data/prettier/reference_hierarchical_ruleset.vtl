define hierarchical ruleset sectorsHierarchy(valuedomain rule Id_2) is
	B = C - D
	errorcode "totalComparedToBanks"
	errorlevel 4;

	N > A + L
	errorcode "totalGeUnal"
	errorlevel 3
end hierarchical ruleset;

sectors_hier_val_unf :=
	check_hierarchy(
		DS_1,
		sectorsHierarchy,
		rule Id_2 non_zero);