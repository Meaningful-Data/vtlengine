define viral propagation VP (variable Va_1) is when "USD" then "USD"; when "EUR" then "EUR"; when "GBP" then "GBP"; when "CHF" then "CHF" end viral propagation;
DS_r := DS_1[calc Me_2 := sum(Me_1 over (partition by Id_1))];
