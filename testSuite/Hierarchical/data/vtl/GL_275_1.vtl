define hierarchical ruleset sectorsHierarchy (variable rule ACCOUNTING_ENTRY) is 
    B = C - D errorcode "totalComparedToBanks" errorlevel 4;
    N >  A + L errorcode "totalGeUnal" errorlevel 3
end hierarchical ruleset;

sectors_hier_val_unf := check_hierarchy(BOP, sectorsHierarchy non_zero);