# Build-time tools for the JupyterLite demo (host Python 3.10+; 3.14 if you also
# build the vtlengine wheel here). Pinned to the 0.8.x line, which ships the
# Pyodide 314 kernel (Python 3.14, PEP 783 `pyemscripten_2026_0` wheels) that the
# vtlengine wasm wheel targets.
jupyterlite-core~=0.8.3
jupyterlite-pyodide-kernel~=0.8.5
jupyterlab~=4.6.3  # the JupyterLab line jupyterlite-core 0.8's `lab` extra pins
